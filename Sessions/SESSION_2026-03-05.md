# Session Changes — 2026-03-05

## Phase 1 — Logo Text Spacing Adjustment

### Goal
Move the "osmiclly" wordmark text closer to the logo icon so the two elements read as a single unified lockup rather than two separate pieces.

### `src/components/layout/Header.tsx`

**Logo Link (`<Link to="/">`)**
- Changed the negative left margin on the `<span>` from `-ml-1` to `-ml-3`
- The span renders `osmiclly` immediately after the `cosmiclly-logo.png` image (which contains the "C" glyph), so the tighter margin closes the gap and makes the full wordmark **Cosmiclly** appear as one cohesive unit

---
